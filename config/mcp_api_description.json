[
  {
    "name": "get_memory_context",
    "description": "Search all memory tiers (working, active, archival, knowledge) for relevant context.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "The search query to match against memory content."
        },
        "max_items": {
          "type": "integer",
          "description": "The maximum number of context items to return.",
          "default": 10
        }
      },
      "required": ["query"]
    }
  },
  {
    "name": "search_knowledge_base",
    "description": "Search for information exclusively within the knowledge base.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "The search query to match against document content."
        }
      },
      "required": ["query"]
    }
  },
  {
    "name": "search_conversations",
    "description": "Search for past conversations in the archival memory.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "The search query to match against conversation content."
        }
      },
      "required": ["query"]
    }
  },
  {
    "name": "add_documents",
    "description": "Add one or more documents to the knowledge base.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "documents": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "content": {
                "type": "string",
                "description": "The content of the document."
              },
              "metadata": {
                "type": "object",
                "description": "A dictionary of metadata for the document."
              }
            },
            "required": ["content"]
          }
        }
      },
      "required": ["documents"]
    }
  },
  {
    "name": "get_current_conversation",
    "description": "Get the messages from the current working memory.",
    "inputSchema": {
      "type": "object",
      "properties": {},
      "required": []
    }
  },
  {
    "name": "end_conversation",
    "description": "End the current conversation and archive it to memory.",
    "inputSchema": {
      "type": "object",
      "properties": {},
      "required": []
    }
  },
  {
    "name": "get_memory_stats",
    "description": "Get comprehensive statistics about the memory system.",
    "inputSchema": {
      "type": "object",
      "properties": {},
      "required": []
    }
  },
  {
    "name": "list_embedding_models",
    "description": "List the available embedding models.",
    "inputSchema": {
      "type": "object",
      "properties": {},
      "required": []
    }
  },
  {
    "name": "switch_embedding_model",
    "description": "Switch to a different embedding model.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "model_name": {
          "type": "string",
          "description": "The name of the model to switch to (e.g., 'fast' or 'default')."
        }
      },
      "required": ["model_name"]
    }
  }
]
