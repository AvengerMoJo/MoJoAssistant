{
  "comment": "External MCP servers — tools from each server are auto-registered into the agent tool pool. Roles declare tool_access by category (e.g. 'browser') to receive matching tools.",
  "servers": [
    {
      "id": "playwright",
      "name": "Playwright MCP",
      "transport": "stdio",
      "command": "npx",
      "args": ["@playwright/mcp@latest", "--headless"],
      "category": "browser",
      "enabled": true
    },
    {
      "id": "tmux",
      "name": "tmux MCP (nickgnd/tmux-mcp)",
      "transport": "stdio",
      "command": "npx",
      "args": ["-y", "tmux-mcp"],
      "category": "terminal",
      "enabled": false,
      "comment": "Persistent tmux session management. Set enabled=true when tmux is available. No global install needed — npx pulls on first use."
    }
  ]
}
