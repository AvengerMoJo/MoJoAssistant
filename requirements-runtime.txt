# MoJoAssistant runtime dependencies
# These are the only deps needed to run the MCP server.
# Local model inference (torch, llama_cpp, etc.) is handled by LMStudio externally.

# Web server
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
starlette>=0.36.0
sse-starlette>=1.8.0
starlette-context>=0.3.6
python-multipart>=0.0.6
slowapi>=0.1.9

# MCP
mcp>=1.0.0

# HTTP clients
httpx>=0.27.0
requests>=2.31.0
aiohttp>=3.9.0

# Data / config
pydantic>=2.0.0
python-dotenv>=1.0.0
anyio>=4.0.0

# Memory / vector store
qdrant-client>=1.7.0
sentence-transformers>=2.6.0

# Utilities
psutil>=5.9.0
GitPython>=3.1.40

# Auth (OAuth 2.1 / Claude Connectors)
PyJWT>=2.8.0
python-jose[cryptography]>=3.3.0
