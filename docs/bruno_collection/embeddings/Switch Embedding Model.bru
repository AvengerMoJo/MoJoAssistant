meta {
  name: Switch Embedding Model
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/embeddings/switch
  body: json
  auth: none
}

headers {
  X-API-Key: {{apiKey}}
}

body:json {
  {
    "model_name": "fast"
  }
}
