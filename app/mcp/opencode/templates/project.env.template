# OpenCode Project Configuration
# Copy this to your project sandbox and customize
# Location: ~/.memory/opencode-sandboxes/<project-name>/.env

# Git repository SSH URL
GIT_URL=git@github.com:username/repo.git

# SSH key for git clone/pull/push (must have read/write access)
# Generate with: ssh-keygen -t ed25519 -f ~/.ssh/myproject-deploy
SSH_KEY_PATH=/home/alex/.ssh/myproject-deploy

# OpenCode web server password (set via OPENCODE_SERVER_PASSWORD env var)
# Generate random: openssl rand -hex 16
OPENCODE_SERVER_PASSWORD=your-secure-password-here

# MCP tool bearer token for authentication
# Generate random: openssl rand -hex 32
MCP_TOOL_BEARER_TOKEN=your-bearer-token-here

# Optional: Custom ports (auto-assigned if not set)
# OPENCODE_PORT=4101
# MCP_TOOL_PORT=5101

# Optional: OpenCode binary path (defaults to 'opencode' in PATH)
# OPENCODE_BIN=/home/alex/.bun/bin/opencode

# Optional: opencode-mcp-tool path
# MCP_TOOL_DIR=/home/alex/Development/Sandbox/opencode-mcp-tool
