meta {
  name: Get Memory Context
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/memory/context
  body: json
  auth: none
}

headers {
  X-API-Key: {{apiKey}}
}

body:json {
  {
    "query": "What is machine learning?",
    "max_items": 10,
    "include_sources": true
  }
}
